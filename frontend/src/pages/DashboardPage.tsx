import { useEffect, useState } from 'react'; import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { api } from '../api/client'; import type { DashboardSummary } from '../types';
const money=(n:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n)
export function DashboardPage(){const [data,setData]=useState<DashboardSummary|null>(null); useEffect(()=>{api.get<DashboardSummary>('/dashboard').then(setData)},[]); if(!data) return <div>Loading...</div>;
return <><header><h1>Accounting Command Center</h1><p>Version 2 - API + persistence-ready</p></header><section className='cards'><article><h3>Receivables</h3><p>{money(data.totalReceivables)}</p><small>Overdue {money(data.overdueReceivables)}</small></article><article><h3>Payables</h3><p>{money(data.totalPayables)}</p><small>Overdue {money(data.overduePayables)}</small></article></section><section className='chart'><h3>Cash Flow</h3><ResponsiveContainer width='100%' height={260}><AreaChart data={data.cashFlow}><CartesianGrid strokeDasharray='3 3'/><XAxis dataKey='month'/><YAxis/><Tooltip/><Area type='monotone' dataKey='incoming' stroke='#10b981' fill='#a7f3d0'/><Area type='monotone' dataKey='outgoing' stroke='#ef4444' fill='#fecaca'/></AreaChart></ResponsiveContainer></section></> }
